# Semantic Keyframe Demo Data

`sub3_largebox_003_template_keyframes.json` is the final deterministic keyframe output generated before retargeting from the human-video-derived SMPL-X trajectory.

`sub3_largebox_003_template_keyframes.event_plan.json` is the accepted declarative VLM plan used to produce it. No InterMimic or retargeted `.pt` trajectory is used by this stage.
