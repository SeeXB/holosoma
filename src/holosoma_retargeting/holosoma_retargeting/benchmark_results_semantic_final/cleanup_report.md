# Semantic branch cleanup

The optimizer now has one semantic algorithmic path: Legacy mean-one Laplacian residual reweighting. Semantic Edge uses equivalent cross-entity edge-endpoint emphasis in that same residual vector and de-duplicates Legacy object-neighbor overlap with `max`. No additive semantic objective, constrained second stage, backtracking, Jacobian balancing, or trust-region re-solve remains callable.

Shared VLM generation/repair, semantic_v2 data, body mapping, original interaction mesh, precision/official evaluators, physical checks, and solver profiling remain. Historical result directories were treated as read-only records.
